with(statementList[argument0])
{
    response = instance_create(x, y, oResponse)
    response.text = other.argument1
    response.link = other.argument2
    ds_list_add(responseList, response)
}

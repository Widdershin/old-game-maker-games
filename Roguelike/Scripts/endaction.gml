if argument0 = "C"
{
    canmove = true
    tryopen = false
    if argument1 = 0        {sendmsg("Nevermind.")}
    else if argument1 = 1   {sendmsg("Success!")}
    else if argument1 = 2   {sendmsg("There is nothing to close there.")}
}
